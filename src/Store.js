const Store = {
    html: document.documentElement,
    body: document.body,
    windowSize: {
        w: window.innerWidth,
        h: window.innerHeight,
        dpr: window.devicePixelRatio
    }
}

module.exports = Store